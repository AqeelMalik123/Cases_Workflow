# Cases Workflow — Frontend Task

A small React + TypeScript workflow app demonstrating:

- List of cases (`/cases`)
- Case detail and status update (`/cases/:caseId`)
- Mock in-memory API
- Client + server validation
- React Query for data fetching & optimistic updates
- React Hook Form for form handling
- Tailwind CSS for styling

---

## Quick start (developer)

Requirements:

- Node 18+ (recommended) / npm 9+
- Git (optional)

Install:

```bash
# install dependencies
npm install
```
